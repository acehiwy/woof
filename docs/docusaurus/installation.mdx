---
description: Docusaurus Installation Instruction
tags:
  - docusaurus
  - documentation
  - tool
---

# Installation

## Requirements

- Node.js version >= 16.14

## Create Docusaurus Project

The easiest way to install Docusaurus is to use the command line tool.

You can run this command anywhere in a new <Color color='var(--secondary-font-color)'> empty repository </Color>
or within an <Color color='var(--secondary-font-color)'> existing repository </Color>

It will create a new directory containing the scaffolded files.

```bash title=
npx create-docusaurus@latest my-website classic
```

:::note

`npx create-docusaurus@latest <name of project> <theme name> [ --typescript ]`

:::

#### or use these command

<Tabs>
<TabItem value="npm" label="npm">

```bash title=
  npm init docusaurus
```

</TabItem>
<TabItem value="yarn" label="yarn">

```bash title=
  yarn create docusaurus
```

</TabItem>
<TabItem value="pnpm" label="pnpm">

```bash title=
  pnpm create docusaurus
```

</TabItem>
</Tabs>

:::tip

Run npx create-docusaurus@latest --help, or check out its [API docs](https://docusaurus.io/docs/api/misc/create-docusaurus) for more information about all available flags.

:::

### Project Structure

```title= showLineNumbers
<Project Directory>
├── blog
│   ├── 2019-05-28-hola.md
│   ├── 2019-05-29-hello-world.md
│   └── 2020-05-30-welcome.md
├── docs
│   ├── doc1.md
│   ├── doc2.md
│   ├── doc3.md
│   └── mdx.md
├── src
│   ├── css
│   │   └── custom.css
│   └── pages
│       ├── styles.module.css
│       └── index.js
├── static
│   └── img
├── docusaurus.config.js
├── package.json
├── README.md
├── sidebars.js
└── yarn.lock
```

- `/blog/` - Contains the blog Markdown files.
  You can delete the directory if you've disabled the blog plugin,
  or you can change its name after setting the path option
- `/docs/` - Contains the Markdown files for the docs.
  You can delete the directory if you've disabled the docs plugin,
  or you can change its name after setting the path option.
- `/src/` - Non-documentation files like <Color color='var(--secondary-font-color)'> pages </Color> or <Color color='var(--secondary-font-color)'> custom React components </Color>.
- `/src/pages` - Any `jsx` `tsx` `mdx` file within this directory will be converted into a website page.
- `/static/` - Static directory. Any contents inside here will be copied into the root of the final build directory
- `/docusaurus.config.js` - A config file containing the site configuration.
- `/sidebars.js` - Used by the documentation to specify the <Color color='var(--primary-font-color)'> order </Color> <Color color='var(--secondary-font-color)'> of documents in the sidebar </Color>

## Running Dev Server

```bash title=
npm run start
```

By default, a browser window will open at [http://localhost:3000](http://localhost:3000).

## Build

```bash title=
npm run build
```

Contents will be generated within the `/build` directory, which can be copied to any static file hosting service

## Update Docusaurus Version

```bash title=
yarn upgrade @docusaurus/core@latest @docusaurus/preset-classic@latest
```

---

# Source

- https://docusaurus.io/docs/installation
